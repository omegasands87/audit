# P0.01 — Identity / Session

Status: FINAL VERIFIED SLICE SPECIFICATION

Objective: register/create account, login, session, logout, refresh, revocation and server-enforced single-login.

Authorities: Core Contract #1; Final PRD; Phase 4 SoT; Final Slice Order/Roadmap.

Owned: User, Account Status, Session. Permission is read from Authorization; commercial access is separate. State covers active/revoked/suspended/blocked session/account outcomes according to Contract #1.

API operations: register/create account, login, refresh, logout, revoke session. Each requires authentication rules defined by Contract #1 and idempotent session revocation where retryable. UI states: loading, invalid credentials, suspended/blocked, success, session revoked, logout.

Security: authentication and single-login enforced server-side. Observability: request/correlation/session identifiers without secrets. Tests: login/logout/refresh/persistence/second-device revocation/suspended-blocked accounts. Exit: identity journey locked.
